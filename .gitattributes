# 行尾统一为 LF：git 存库一律用 LF，避免 Windows/macOS/Linux 互相干扰
* text=auto
# Python/文档/配置文件 checkout 时也用 LF（不再出现 CRLF 警告）
*.py text eol=lf
*.md text eol=lf
*.toml text eol=lf
*.lock text eol=lf
*.gitignore text eol=lf
*.gitattributes text eol=lf